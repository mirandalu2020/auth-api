{
  "name": "server",
  "version": "1.0.0",
  "description": "Being able to login is great. But controlling access at a more granular level is vital to creating a scalable system. In this lab, you will implement Role Based Access Control (RBAC) using an Access Control List (ACL), allowing to not only restrict access to routes for valid users, but also based on the individual permissions we give each user.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "db:config": "sequelize init:config",
    "db:create": "sequelize db:create",
    "test-watch": "NODE_ENV=test jest --verbose --coverage --watchAll",
    "test": "NODE_ENV=test jest  --verbose --coverage",
    "dev": "nodemon index.js"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "base-64": "^1.0.0",
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "eslint": "^8.29.0",
    "express": "^4.17.1",
    "jest": "^26.6.3",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.6.0",
    "sequelize": "^6.6.2",
    "sequelize-cli": "^6.2.0",
    "sqlite3": "^5.0.2",
    "supertest": "^6.1.3"
  },
  "devDependencies": {}
}
